# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# This is where you put your own keybinds. Be Mindful to check as well ~/.config/hypr/configs/Keybinds.conf to avoid conflict
# if you think I should replace the Pre-defined Keybinds in ~/.config/hypr/configs/Keybinds.conf , submit an issue or let me know in DC and present me a valid reason as to why, such as conflicting with global shortcuts, etc etc

# See https://wiki.hyprland.org/Configuring/Keywords/ for more settings and variables
# See also Laptops.conf for laptops keybinds 

# /* ---- ✴️ Variables ✴️ ---- */  #
$mainMod = SUPER
$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts
$UserConfigs = $HOME/.config/hypr/UserConfigs

# settings for User defaults apps - set your default terminal and file manager on this file
source= $UserConfigs/01-UserDefaults.conf

# common shortcuts
bindr = $mainMod, $mainMod_L, exec, pkill -9 tofi 2>/dev/null || ~/.local/bin/tofi-blazing # Super Key toggles tofi menu
bind = $mainMod, D, exec, pkill -9 tofi 2>/dev/null || ~/.local/bin/tofi-blazing # Main Menu (APP Launcher)
bind = $mainMod SHIFT, F, exec, ~/.local/bin/tofi-files-live # Live file search with tofi
bind = $mainMod, B, exec, xdg-open "https://"  # default browser
bind = $mainMod, A, exec, pkill rofi || true && ags -t 'overview' # desktop overview (if installed)

# Unbind default tab workspace cycling (from configs/Keybinds.conf:92-93)
unbind = $mainMod, tab
unbind = $mainMod SHIFT, tab
# bind = $mainMod, tab, hyprexpo:expo, toggle # HyprExpo overview mode

# Override period/comma to cycle through ALL workspaces (including empty ones)
unbind = $mainMod, period
unbind = $mainMod, comma
bind = $mainMod, period, workspace, r+1  # next workspace (relative, includes empty)
bind = $mainMod, comma, workspace, r-1   # previous workspace (relative, includes empty)

# Override mouse wheel to cycle through ALL workspaces (including empty ones)
unbind = $mainMod, mouse_down
unbind = $mainMod, mouse_up
bind = $mainMod, mouse_down, workspace, r-1  # previous workspace with mouse wheel (includes empty)
bind = $mainMod, mouse_up, workspace, r+1    # next workspace with mouse wheel (includes empty)


# Mouse side buttons to move window to next/previous workspace
bind = $mainMod, mouse:275, movetoworkspace, r+1  # SUPER + side button (back) = move window to next workspace
bind = $mainMod, mouse:276, movetoworkspace, r-1  # SUPER + extra button (forward) = move window to previous workspace

# Move window to next/previous workspace and follow (hold/tap-able)
binde = $mainMod SHIFT, period, movetoworkspace, +1  # move window to next workspace and follow (repeatable)
binde = $mainMod SHIFT, comma, movetoworkspace, -1   # move window to previous workspace and follow (repeatable)
bind = $mainMod, Return, exec, $term  #terminal
bind = $mainMod, E, exec, $files #file manager
bind = $mainMod, Z, exec, ~/.local/bin/zed # Zed editor
bind = $mainMod, C, exec, cursor # Cursor editor
bind = CTRL SHIFT, Escape, exec, $term -e btop --utf-force # system monitor (btop)


# FEATURES / EXTRAS
bind = $mainMod, H, exec, $scriptsDir/KeyHints.sh # help / cheat sheet
bind = $mainMod ALT, R, exec, $scriptsDir/Refresh.sh # Refresh waybar, swaync, rofi
bind = $mainMod ALT, E, exec, $scriptsDir/RofiEmoji.sh # emoji menu
bind = $mainMod, S, exec, $scriptsDir/RofiSearch.sh # Google search using rofi
unbind = $mainMod SHIFT, S # Unbind the default swappy screenshot
bind = $mainMod SHIFT, S, exec, $scriptsDir/ScreenShot.sh --area # screenshot area (override swappy default)
bind = $mainMod ALT, O, exec, $scriptsDir/ChangeBlur.sh # Toggle blur settings 
bind = $mainMod SHIFT, G, exec, $scriptsDir/GameMode.sh # Toggle animations ON/OFF
bind = $mainMod ALT, L, exec, $scriptsDir/ChangeLayout.sh # Toggle Master or Dwindle Layout
bind = $mainMod, V, exec, $scriptsDir/ClipManager.sh # Clipboard Manager (simpler keybind)
bind = $mainMod ALT, V, exec, $scriptsDir/ClipManager.sh # Clipboard Manager
bind = $mainMod CTRL, R, exec, $scriptsDir/RofiThemeSelector.sh # KooL Rofi Menu Theme Selector
bind = $mainMod CTRL SHIFT, R, exec, pkill rofi || true && $scriptsDir/RofiThemeSelector-modified.sh # modified Rofi Theme Selector

bind = $mainMod, F, fullscreen, 1 # fake full screen
bind = $mainMod CTRL, F, fullscreen # whole full screen (moved from SHIFT to CTRL to avoid conflict)
bind = $mainMod, SPACE, togglefloating, #Float Mode
bind = $mainMod ALT, SPACE, exec, hyprctl dispatch workspaceopt allfloat #All Float Mode
bind = $mainMod SHIFT, Return, exec, [float; move 15% 5%; size 70% 60%] $term # Dropdown terminal

# Desktop zooming or magnifier
bind = $mainMod ALT, mouse_down, exec, hyprctl keyword cursor:zoom_factor "$(hyprctl getoption cursor:zoom_factor | awk 'NR==1 {factor = $2; if (factor < 1) {factor = 1}; print factor * 2.0}')"
bind = $mainMod ALT, mouse_up, exec, hyprctl keyword cursor:zoom_factor "$(hyprctl getoption cursor:zoom_factor | awk 'NR==1 {factor = $2; if (factor < 1) {factor = 1}; print factor / 2.0}')" 

## NOTES for ja (Hyprland version 0.39 (Ubuntu 24.04))
#bind = $mainMod ALT, mouse_down, exec, hyprctl keyword misc:cursor_zoom_factor "$(hyprctl getoption misc:cursor_zoom_factor | awk 'NR==1 {factor = $2; if (factor < 1) {factor = 1}; print factor * 2.0}')"
#bind = $mainMod ALT, mouse_up, exec, hyprctl keyword misc:cursor_zoom_factor "$(hyprctl getoption misc:cursor_zoom_factor | awk 'NR==1 {factor = $2; if (factor < 1) {factor = 1}; print factor / 2.0}')" 

# Waybar / Bar related
bind = $mainMod CTRL ALT, B, exec, pkill -SIGUSR1 waybar # Toggle hide/show waybar
bind = $mainMod CTRL, B, exec, $scriptsDir/WaybarStyles.sh # Waybar Styles Menu
bind = $mainMod ALT, B, exec, $scriptsDir/WaybarLayout.sh # Waybar Layout Menu
bind = $mainMod SHIFT, B, exec, pkill waybar && sleep 0.5 && waybar & # Reload/restart waybar

# FEATURES / EXTRAS (UserScripts)
# bind = $mainMod SHIFT, M, exec, $UserScripts/RofiBeats.sh # online music using rofi (DISABLED)
bind = $mainMod SHIFT, M, exec, $UserScripts/ToggleMicMonitor.sh # Toggle mic monitoring
bind = $mainMod, M, exec, ~/.local/bin/niflveil minimize # Minimize current window
binde = $mainMod ALT, M, exec, $UserScripts/MicMonitorVolume.sh up # Increase mic monitor volume (repeatable)
binde = $mainMod SHIFT ALT, M, exec, $UserScripts/MicMonitorVolume.sh down # Decrease mic monitor volume (repeatable)
bind = $mainMod, W, killactive, # Close active window (graceful)
bind = $mainMod SHIFT, W, exec, ~/.local/bin/niflveil minimize # Minimize window (NiflVeil)
bind = $mainMod CTRL, W, exec, $scriptsDir/KillActiveProcess.sh # Kill active process (force)
bind = $mainMod, R, exec, $UserScripts/RestartWindow.sh # Restart active window
binde = CTRL ALT, W, exec, $UserScripts/WallpaperRandom.sh # Random wallpapers (repeatable)
binde = CTRL ALT, left, exec, $UserScripts/WallpaperNavigate.sh prev # Previous wallpaper (repeatable)
binde = CTRL ALT, right, exec, $UserScripts/WallpaperNavigate.sh next # Next wallpaper (repeatable)
bind = $mainMod, O, exec, $UserScripts/ToggleOpacity.sh # Toggle window transparency
bind = $mainMod SHIFT, K, exec, $UserScripts/TofiKeybindSearch.sh # search and execute keybinds via tofi
bind = $mainMod SHIFT, A, exec, $scriptsDir/Animations.sh #hyprland animations menu
bind = $mainMod SHIFT, O, exec, $UserScripts/ZshChangeTheme.sh # Change oh-my-zsh theme
bind = $mainMod ALT, C, exec, $UserScripts/RofiCalc.sh # calculator (qalculate)

# Notification controls
bind = $mainMod CTRL, N, exec, swaync-client -C # Clear all notifications
bind = $mainMod CTRL SHIFT, N, exec, swaync-client -d # Close notification panel

# Window switching with Alt+Tab - opens NiflVeil restore menu if minimized windows exist
bind = ALT, tab, exec, $UserScripts/NiflVeilRestore.sh
bind = ALT SHIFT, tab, cyclenext, prev
bind = ALT, Caps_Lock, cyclenext, prev

# For passthrough keyboard into a VM
# bind = $mainMod ALT, P, submap, passthru
#submap = passthru
# to unbind
#bind = $mainMod ALT, P, submap, reset
#submap = reset

# --- Workspaces 11-20 (Monitor 2) with SUPER+F1-F10 ---
# Switch to workspaces 11-20 with SUPER+F1-F10
bind = $mainMod, F1, workspace, 11   # SUPER+F1 → workspace 11
bind = $mainMod, F2, workspace, 12   # SUPER+F2 → workspace 12
bind = $mainMod, F3, workspace, 13   # SUPER+F3 → workspace 13
bind = $mainMod, F4, workspace, 14   # SUPER+F4 → workspace 14
bind = $mainMod, F5, workspace, 15   # SUPER+F5 → workspace 15
bind = $mainMod, F6, workspace, 16   # SUPER+F6 → workspace 16
bind = $mainMod, F7, workspace, 17   # SUPER+F7 → workspace 17
bind = $mainMod, F8, workspace, 18   # SUPER+F8 → workspace 18
bind = $mainMod, F9, workspace, 19   # SUPER+F9 → workspace 19
bind = $mainMod, F10, workspace, 20  # SUPER+F10 → workspace 20
bind = $mainMod, F11, workspace, 21  # SUPER+F11 → workspace 21 (bonus)
bind = $mainMod, F12, workspace, 22  # SUPER+F12 → workspace 22 (bonus)

# Move active window to workspaces 11-20 with SUPER+SHIFT+F1-F10
bind = $mainMod SHIFT, F1, movetoworkspace, 11   # SUPER+SHIFT+F1 → move to workspace 11
bind = $mainMod SHIFT, F2, movetoworkspace, 12   # SUPER+SHIFT+F2 → move to workspace 12
bind = $mainMod SHIFT, F3, movetoworkspace, 13   # SUPER+SHIFT+F3 → move to workspace 13
bind = $mainMod SHIFT, F4, movetoworkspace, 14   # SUPER+SHIFT+F4 → move to workspace 14
bind = $mainMod SHIFT, F5, movetoworkspace, 15   # SUPER+SHIFT+F5 → move to workspace 15
bind = $mainMod SHIFT, F6, movetoworkspace, 16   # SUPER+SHIFT+F6 → move to workspace 16
bind = $mainMod SHIFT, F7, movetoworkspace, 17   # SUPER+SHIFT+F7 → move to workspace 17
bind = $mainMod SHIFT, F8, movetoworkspace, 18   # SUPER+SHIFT+F8 → move to workspace 18
bind = $mainMod SHIFT, F9, movetoworkspace, 19   # SUPER+SHIFT+F9 → move to workspace 19
bind = $mainMod SHIFT, F10, movetoworkspace, 20  # SUPER+SHIFT+F10 → move to workspace 20
bind = $mainMod SHIFT, F11, movetoworkspace, 21  # SUPER+SHIFT+F11 → move to workspace 21
bind = $mainMod SHIFT, F12, movetoworkspace, 22  # SUPER+SHIFT+F12 → move to workspace 22

# Move active window silently to workspaces 11-20 with SUPER+CTRL+F1-F10
bind = $mainMod CTRL, F1, movetoworkspacesilent, 11   # SUPER+CTRL+F1 → silent move to workspace 11
bind = $mainMod CTRL, F2, movetoworkspacesilent, 12   # SUPER+CTRL+F2 → silent move to workspace 12
bind = $mainMod CTRL, F3, movetoworkspacesilent, 13   # SUPER+CTRL+F3 → silent move to workspace 13
bind = $mainMod CTRL, F4, movetoworkspacesilent, 14   # SUPER+CTRL+F4 → silent move to workspace 14
bind = $mainMod CTRL, F5, movetoworkspacesilent, 15   # SUPER+CTRL+F5 → silent move to workspace 15
bind = $mainMod CTRL, F6, movetoworkspacesilent, 16   # SUPER+CTRL+F6 → silent move to workspace 16
bind = $mainMod CTRL, F7, movetoworkspacesilent, 17   # SUPER+CTRL+F7 → silent move to workspace 17
bind = $mainMod CTRL, F8, movetoworkspacesilent, 18   # SUPER+CTRL+F8 → silent move to workspace 18
bind = $mainMod CTRL, F9, movetoworkspacesilent, 19   # SUPER+CTRL+F9 → silent move to workspace 19
bind = $mainMod CTRL, F10, movetoworkspacesilent, 20  # SUPER+CTRL+F10 → silent move to workspace 20
bind = $mainMod CTRL, F11, movetoworkspacesilent, 21  # SUPER+CTRL+F11 → silent move to workspace 21
bind = $mainMod CTRL, F12, movetoworkspacesilent, 22  # SUPER+CTRL+F12 → silent move to workspace 22

# NiflVeil - Window minimize manager (using tofi with wallust theming)
# bind = $mainMod, N, exec, ~/.local/bin/niflveil minimize          # Minimize current window (DISABLED - conflicts with notification panel)
bind = $mainMod, I, exec, $UserScripts/NiflVeilRestore.sh         # Open restore menu (tofi)
bind = $mainMod, U, exec, ~/.local/bin/niflveil restore-last      # Restore last minimized window
bind = $mainMod SHIFT, U, exec, ~/.local/bin/niflveil restore-all # Restore all minimized windows

bindm = $mainMod, mouse:272, movewindow # left click
bindm = $mainMod, mouse:273, resizewindow # right click
